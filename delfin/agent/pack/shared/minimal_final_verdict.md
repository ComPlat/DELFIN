# Minimal Final Verdict

Each active agent must return:

- `status`: `approve` / `approve_with_risks` / `reject`
- `key findings`
- `open risks`
- `recommended next step`

If the agent has a longer required output schema, include this verdict block at
the end.
